#include "MiniginPCH.h"
#include "GameInfo.h"

void GameInfo::SetGameState(GameState gameState)
{
	m_GameState = gameState;
}

void GameInfo::SetGameMode(GameMode gameMode)
{
	m_GameMode = gameMode;
}
